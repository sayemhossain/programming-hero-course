// declare variable based on the name of object property
const myObject = { x: 2, y: 50, z: 400 };
const { x } = myObject;
// console.log(x);

// destructuring array
const [p, q] = [23, 56, 76, 54];
console.log(p, q);
